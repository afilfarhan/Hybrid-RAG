# Test configuration for Hybrid RAG system.

[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
asyncio_mode = auto
markers =
    unit: unit tests
    integration: integration tests
    slow: slow running tests
